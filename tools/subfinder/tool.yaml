desc: "子域名发现"
command: "{bin} -dL {targets_file} -all -json -silent -timeout 30 -proxy http://192.168.166.166:16666"
use_on:
  RootDomain:
    desc: "从根域名发现子域名"
    params:
      targets_file: "{value}"
