desc: "快速端口发现"
command: "{bin} -list {targets_file} -top-ports 1000 -silent -json"
use_on:
  IP:
    desc: "扫描 IP Top1000 端口(迭代, -host 逐个)"
    command: "{bin} -host {ip} -top-ports 1000 -silent -json"
    params:
      ip: "{ip}"
