# ============================================================
# GraphPT — Python 依赖
# 采集引擎 + 认知引擎共用
# ============================================================

# ---- Celery 任务队列 ----
celery[redis]>=5.3,<6.0
flower>=2.0,<3.0
redis>=5.0,<6.0

# ---- Neo4j 图数据库 ----
neo4j>=5.15,<6.0

# ---- HTTP / 采集 ----
httpx>=0.25,<1.0
aiohttp>=3.9,<4.0

# ---- 浏览器自动化 (L2 采集) ----
playwright>=1.40,<2.0

# ---- AI SDK ----
claude-agent-sdk>=0.1.0
# OpenAI 兼容客户端（DeepSeek 等）
openai>=1.0,<2.0

# ---- 工具依赖 ----
pyyaml>=6.0,<7.0
python-dotenv>=1.0,<2.0
# 中文指纹名→拼音，用于 nuclei 指纹驱动的 tag 匹配（软依赖：缺失则中文名走盲扫兜底）
pypinyin>=0.50,<1.0

# ---- 数据解析 ----
lxml>=4.9,<6.0
beautifulsoup4>=4.12,<5.0

# ---- 日志 / 监控 ----
structlog>=23.0,<25.0

# ---- Web Admin ----
fastapi>=0.109,<1.0
uvicorn[standard]>=0.27,<1.0

# ---- CLI ----
rich>=13.0,<14.0
click>=8.1,<9.0
