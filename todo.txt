GraphPT 待办事项
================

🔴 紧急（影响基本可用性）
✅ 1. 断点续扫：_mark_scanned_batch 每 chunk 立即写 ScanRun，同一 try 块保证原子性。
✅ 2. _mark_scanned_batch 错误不再吞：打日志 + re-raise，失败 chunk 下次重扫。
✅ 3. 活性检测：proc.poll() 轮询 + 日志文件增长检测，5 分钟无产出 → kill，替代死等。
✅ 4. Layer 2 vs Layer 4 httpx 拆分：httpx:subdomain / httpx:port 各自查各自的节点。

🟡 重要（提升效率）
✅ 5. 适配器拆分：15 个适配器移至 tools/<name>/adapter.py，基类 + 自动发现保留在 adapter.py。
✅ 6. gobuster DNS/VHOST 接入调度链：gobuster:dns (Layer 1) + gobuster:vhost (Layer 3) + gobuster dir (Layer 5)。
✅ 7. Web 面板实时日志查看：Tool 选择器 + 文件列表 + 内容阅读 + 3 秒自动刷新。
✅ 8. Neo4j write_batch 优化：开一个 session，所有 finding 共享，代替每个 write_* 单独开。
✅ 9. 提取器统一：pipeline.py _BATCH_TARGETS 硬编码字典 → tools/<name>/targets.yaml + 自动加载 + transform 注册表。
✅ 10. 资产管理 API：GET/POST/PATCH/DELETE + 根域名种子 + 前端统计展示。
✅ 11. 资源隔离：动态槽位 = max(1, GRAPHPT_CONCURRENCY / 活跃资产数)，env 可配。

🟢 规划（长期方向）
12. 报告导出：PDF/HTML/Markdown 报告生成。
