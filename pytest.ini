[pytest]
addopts = -m "not integration and not slow and not external_tool"
markers =
    unit: fast tests that do not require external services or tools
    integration: tests that exercise multiple components or real services
    slow: tests that may exceed the default quick feedback budget
    external_tool: tests that require local binaries, network access, or host services
